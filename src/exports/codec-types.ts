/**
 * Codec type definitions for the typed-json extension.
 *
 * The pack metadata references this entrypoint via
 * `import: { package: 'prisma-orm-extension-typed-json/codec-types' }` so the emitter threads
 * `CodecTypes` into `contract.d.ts`.
 */

export type { CodecTypes } from '../types/codec-types.js';
